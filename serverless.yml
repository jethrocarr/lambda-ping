service: lambda-ping

provider:
  name: aws
  runtime: nodejs20.x
  environment:
    # Add any necessary environment variables here

plugins:
  - serverless-plugin-layers
  - serverless-offline

functions:
  handler:
    handler: handler.handler
    events:
      - http:
          path: example
          method: get